# Csound Plugins (windows)

## Installation

copy the .dll files to csound's opcodes folder. If csound was installed via
the official installer, this folder is normally set to:

C:\Program Files\Csound6_x64\plugins64

(check the environmental variable OPCODE6DIR64)

